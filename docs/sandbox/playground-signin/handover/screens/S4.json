{
  "$description": "Per-screen handoff spec for \"PIN entry\" (S4) in flow \"playground-signin\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S4",
  "name": "PIN entry",
  "flow": "playground-signin",
  "route": "index.html?v=1&step=pin&device=phone",
  "routeBare": "index.html?v=1&step=pin&device=phone&bare=1",
  "purpose": "Verify the selected educator with a 4-digit PIN.",
  "entry": "After selecting an educator on S2",
  "exit": "→ S5 Room select (cold start) / → Room Hub (returning) on correct PIN; lockout after repeated failures (e-locked)",
  "platforms": [
    "ios",
    "android"
  ],
  "figma": null,
  "layout": [
    "Centred group (brand + header + dots + keypad)",
    "Greeting header",
    "4 PIN dots",
    "On-screen numeric keypad with delete"
  ],
  "copy": [
    {
      "element": "Title",
      "text": "Hi <FirstName>"
    },
    {
      "element": "Subtitle",
      "text": "Enter your PIN to continue"
    },
    {
      "element": "Incorrect-attempt message",
      "text": "Incorrect PIN — try again"
    }
  ],
  "components": [
    "ds-pin-keypad"
  ],
  "tokens": [
    "--sd-colour-feedback-error-default",
    "--sd-colour-text-primary"
  ],
  "redlines": [
    {
      "element": "PIN length",
      "spec": "4 digits"
    },
    {
      "element": "Keypad",
      "spec": "max-width 300, centred"
    }
  ],
  "behaviour": [
    "PIN auto-submits once the 4th digit is entered (no submit button).",
    "Incorrect PIN shakes the dots and shows the incorrect-attempt message; attempts increment.",
    "Repeated failures lead to a locked state (e-locked).",
    "Demo PIN in the prototype: 1234.",
    "Back returns to the educator list (or same-day for returning users)."
  ],
  "states": [
    "default",
    "entering",
    "incorrect",
    "locked:e-locked"
  ],
  "a11y": [
    "Keypad keys are ≥44px labelled buttons; delete key labelled.",
    "PIN progress is announced; the incorrect message is announced (role=alert).",
    "Avoid relying on colour alone for the incorrect state (message + shake)."
  ],
  "acceptance": [
    {
      "id": "S4-AC1",
      "text": "Entering the 4th digit auto-submits without a button.",
      "assert": "pin.length==4 -> submit fires automatically",
      "screen": "S4"
    },
    {
      "id": "S4-AC2",
      "text": "A correct PIN advances to S5 (cold start).",
      "assert": "submit(correct) AND scenario!=return -> route==S5",
      "screen": "S4"
    },
    {
      "id": "S4-AC3",
      "text": "An incorrect PIN shows the incorrect message and increments attempts.",
      "assert": "submit(incorrect) -> incorrectMsg.visible AND attempts++",
      "screen": "S4"
    }
  ]
}
