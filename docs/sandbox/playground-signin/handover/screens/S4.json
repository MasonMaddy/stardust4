{
  "$description": "Per-screen handoff spec for \"PIN entry\" (S4) in flow \"playground-signin\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S4",
  "name": "PIN entry",
  "flow": "playground-signin",
  "route": "version-0.3/index.html?v=3&step=pin&device=phone",
  "routeBare": "version-0.3/index.html?v=3&step=pin&device=phone&bare=1",
  "purpose": "Verify the selected educator with a 4-digit PIN.",
  "entry": "After selecting an educator on S2",
  "exit": "→ S5 Room select (cold start) / → Room Hub (returning) on correct PIN; → S7 via 'Use password'; lockout after 5 failures (e-locked)",
  "platforms": [
    "ios",
    "android"
  ],
  "figma": null,
  "layout": [
    "Teal hero — brand mark is now the EDUCATOR'S PHOTO + greeting",
    "4 PIN dots (ring → filled, pop on entry)",
    "Circular on-screen numeric keypad with delete",
    "'Forgot PIN? Use password' link"
  ],
  "copy": [
    {
      "element": "Title",
      "text": "Hi <FirstName>"
    },
    {
      "element": "Subtitle",
      "text": "Enter your PIN to continue"
    },
    {
      "element": "Remaining attempts",
      "text": "Incorrect PIN — <n> tries left"
    },
    {
      "element": "Password fallback link",
      "text": "Forgot PIN? Use password"
    }
  ],
  "components": [
    "ds-pin-keypad"
  ],
  "tokens": [
    "--sd-colour-feedback-error-default",
    "--sd-colour-text-primary",
    "--sd-colour-action-primary"
  ],
  "redlines": [
    {
      "element": "PIN length",
      "spec": "4 digits, auto-submit"
    },
    {
      "element": "Keypad",
      "spec": "circular keys, max-width ~300 (phone), centred"
    },
    {
      "element": "Lockout",
      "spec": "5 incorrect attempts → e-locked"
    }
  ],
  "behaviour": [
    "PIN auto-submits once the 4th digit is entered (no submit button).",
    "Incorrect PIN shakes the dots and shows 'Incorrect PIN — <n> tries left'; attempts increment.",
    "After 5 incorrect attempts the screen shows the locked panel (e-locked) with a Switch educator action.",
    "'Forgot PIN? Use password' routes to S7 (password fallback).",
    "Demo PIN in the prototype: 1234.",
    "Back returns to the educator list (or same-day for returning users)."
  ],
  "states": [
    "default",
    "entering",
    "incorrect",
    "locked:e-locked"
  ],
  "a11y": [
    "Keypad keys are ≥44px labelled buttons; delete key labelled.",
    "PIN progress is announced; the incorrect message is announced (role=alert).",
    "Don't rely on colour alone for the incorrect state (message + shake + remaining count)."
  ],
  "acceptance": [
    {
      "id": "S4-AC1",
      "text": "Entering the 4th digit auto-submits without a button.",
      "assert": "pin.length==4 -> submit fires automatically",
      "screen": "S4"
    },
    {
      "id": "S4-AC2",
      "text": "A correct PIN advances to S5 (cold start).",
      "assert": "submit(correct) AND scenario!=return -> route==S5",
      "screen": "S4"
    },
    {
      "id": "S4-AC3",
      "text": "Each incorrect PIN shows remaining tries and increments attempts.",
      "assert": "submit(incorrect) -> message=='Incorrect PIN — '+(5-attempts)+' tries left' AND attempts++",
      "screen": "S4"
    },
    {
      "id": "S4-AC4",
      "text": "Five incorrect attempts lock the screen.",
      "assert": "attempts>=5 -> e-locked panel visible AND keypad hidden",
      "screen": "S4"
    },
    {
      "id": "S4-AC5",
      "text": "The password link routes to the password fallback (S7).",
      "assert": "tap(usePassword) -> route==S7",
      "screen": "S4"
    }
  ]
}
