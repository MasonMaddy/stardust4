{
  "$description": "Per-screen handoff spec for \"Educator password (PIN fallback)\" (S7) in flow \"playground-signin\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S7",
  "name": "Educator password (PIN fallback)",
  "flow": "playground-signin",
  "route": "version-0.3/index.html?v=3&step=edupass&device=phone",
  "routeBare": "version-0.3/index.html?v=3&step=edupass&device=phone&bare=1",
  "purpose": "Let an educator sign in with a password when they can't use their PIN.",
  "entry": "From 'Forgot PIN? Use password' on S4",
  "exit": "→ S5 Room select (cold start) / → Room Hub (returning) on success; 'Use PIN instead' → S4",
  "platforms": [
    "ios",
    "android"
  ],
  "figma": null,
  "layout": [
    "Teal hero (educator photo)",
    "'Enter your password' header (+ 'Signing in as <Name>')",
    "Password field (reveal toggle)",
    "Sign in button",
    "'Use PIN instead' link"
  ],
  "copy": [
    {
      "element": "Title",
      "text": "Enter your password"
    },
    {
      "element": "Subtitle",
      "text": "Signing in as <Name>"
    },
    {
      "element": "Field label",
      "text": "Password"
    },
    {
      "element": "Primary button",
      "text": "Sign in"
    },
    {
      "element": "PIN link",
      "text": "Use PIN instead"
    }
  ],
  "components": [
    "ds-input",
    "ds-btn"
  ],
  "tokens": [
    "--sd-radius-lg",
    "--sd-colour-action-primary"
  ],
  "redlines": [
    {
      "element": "Field",
      "spec": "52px, radius-lg, reveal toggle"
    },
    {
      "element": "Button",
      "spec": "disabled until the password field has content"
    }
  ],
  "behaviour": [
    "Sign in enabled only when the password field is non-empty.",
    "On success routes onward exactly like a correct PIN (S5 cold start / hub returning).",
    "'Use PIN instead' returns to S4."
  ],
  "states": [
    "default",
    "filled",
    "disabled"
  ],
  "a11y": [
    "Labelled input; reveal toggle is a labelled button; both links reachable."
  ],
  "acceptance": [
    {
      "id": "S7-AC1",
      "text": "Sign in is disabled until the password has content.",
      "assert": "signin.enabled == (password.length>0)",
      "screen": "S7"
    },
    {
      "id": "S7-AC2",
      "text": "Success routes onward like a correct PIN.",
      "assert": "submit(valid) AND scenario!=return -> route==S5",
      "screen": "S7"
    },
    {
      "id": "S7-AC3",
      "text": "'Use PIN instead' returns to S4.",
      "assert": "tap(usePin) -> route==S4",
      "screen": "S7"
    }
  ]
}
