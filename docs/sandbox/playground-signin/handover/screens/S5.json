{
  "$description": "Per-screen handoff spec for \"Room select\" (S5) in flow \"playground-signin\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S5",
  "name": "Room select",
  "flow": "playground-signin",
  "route": "version-0.4/index.html?v=3&step=rooms&device=phone",
  "routeBare": "version-0.4/index.html?v=3&step=rooms&device=phone&bare=1",
  "purpose": "Choose the room the educator is working in today.",
  "entry": "After a correct PIN (cold start) or Add educator",
  "exit": "→ Room Hub on Continue",
  "platforms": [
    "ios",
    "android"
  ],
  "figma": null,
  "layout": [
    "Teal hero (educator photo), back nav",
    "Room rows — photo thumbnail + name + ratio, selectable",
    "Continue button (pinned to the bottom)",
    "Whole page scrolls; hero scrolls away"
  ],
  "copy": [
    {
      "element": "Title",
      "text": "Select your room"
    },
    {
      "element": "Subtitle",
      "text": "Where are you working today?"
    },
    {
      "element": "Needs-attention label",
      "text": "· Needs attention"
    },
    {
      "element": "Button (no room selected)",
      "text": "Select a room"
    },
    {
      "element": "Button (room selected)",
      "text": "Continue to <Room>"
    }
  ],
  "components": [
    "ds-card",
    "ds-btn"
  ],
  "tokens": [
    "--sd-radius-lg",
    "--sd-colour-action-primary",
    "--sd-colour-orange-500"
  ],
  "redlines": [
    {
      "element": "Room row",
      "spec": "selectable card; 46px photo thumbnail + name + 'Ratio x:1'; selected = teal border + green check"
    },
    {
      "element": "Needs-attention",
      "spec": "amber (orange-500) left accent + label"
    },
    {
      "element": "Disabled / closed",
      "spec": "greyed, dashed border, status chip (e.g. 'At capacity'), not selectable"
    },
    {
      "element": "Continue",
      "spec": "disabled until a room is selected; pinned bottom"
    }
  ],
  "behaviour": [
    "Selecting a room enables Continue and updates its label to 'Continue to <Room>'.",
    "Continue routes to the Room Hub.",
    "Needs-attention rooms (under ratio) are selectable; disabled rooms (closed / at capacity) are not — closed rooms cannot be entered.",
    "Room thumbnails are placeholder photos with a letter-tile fallback if the image can't load."
  ],
  "states": [
    "default",
    "room-selected",
    "needs-attention",
    "disabled",
    "empty:e-norooms"
  ],
  "a11y": [
    "Room rows are radio-like selectable buttons (name + ratio + status); selected exposed non-visually; disabled rooms are not focusable as actions."
  ],
  "acceptance": [
    {
      "id": "S5-AC1",
      "text": "Continue is disabled until a selectable room is chosen.",
      "assert": "continue.enabled == (room != null)",
      "screen": "S5"
    },
    {
      "id": "S5-AC2",
      "text": "Continue routes to the Room Hub with the selected room in context.",
      "assert": "tap(continue) -> route==hub AND ctx.room==selected",
      "screen": "S5"
    },
    {
      "id": "S5-AC3",
      "text": "Disabled rooms cannot be selected.",
      "assert": "tap(room.disabled) -> room==unchanged",
      "screen": "S5"
    }
  ]
}
