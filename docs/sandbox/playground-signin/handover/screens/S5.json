{
  "$description": "Per-screen handoff spec for \"Room select\" (S5) in flow \"playground-signin\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S5",
  "name": "Room select",
  "flow": "playground-signin",
  "route": "index.html?v=1&step=rooms&device=phone",
  "routeBare": "index.html?v=1&step=rooms&device=phone&bare=1",
  "purpose": "Choose the room the educator is working in today.",
  "entry": "After a correct PIN (cold start) or Add educator",
  "exit": "→ Room Hub on Continue",
  "platforms": [
    "ios",
    "android"
  ],
  "figma": null,
  "layout": [
    "Header: title + subtitle, back nav",
    "List of room rows (name + ratio, selectable)",
    "Continue button (pinned)"
  ],
  "copy": [
    {
      "element": "Title",
      "text": "Select your room"
    },
    {
      "element": "Subtitle",
      "text": "Where are you working today?"
    },
    {
      "element": "Button (no room selected)",
      "text": "Select a room"
    },
    {
      "element": "Button (room selected)",
      "text": "Continue to <Room>"
    }
  ],
  "components": [
    "ds-card",
    "ds-btn"
  ],
  "tokens": [
    "--sd-radius-lg",
    "--sd-colour-action-primary"
  ],
  "redlines": [
    {
      "element": "Room row",
      "spec": "selectable card with ratio badge; selected = green treatment"
    },
    {
      "element": "Continue",
      "spec": "disabled until a room is selected"
    }
  ],
  "behaviour": [
    "Selecting a room enables Continue and updates its label to 'Continue to <Room>'.",
    "Continue routes to the Room Hub.",
    "A closed room shows a warning but can still be entered (see e-closed)."
  ],
  "states": [
    "default",
    "room-selected",
    "empty:e-norooms",
    "warning:e-closed"
  ],
  "a11y": [
    "Room rows are radio-like selectable buttons with names + ratio; selected state is exposed non-visually."
  ],
  "acceptance": [
    {
      "id": "S5-AC1",
      "text": "Continue is disabled until a room is selected.",
      "assert": "continue.enabled == (room != null)",
      "screen": "S5"
    },
    {
      "id": "S5-AC2",
      "text": "Continue routes to the Room Hub with the selected room in context.",
      "assert": "tap(continue) -> route==hub AND ctx.room==selected",
      "screen": "S5"
    }
  ]
}
