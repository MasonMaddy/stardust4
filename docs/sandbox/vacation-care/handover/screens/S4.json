{
  "$description": "Per-screen handoff spec for \"Forms tracking table\" (S4) in flow \"vacation-care\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S4",
  "name": "Forms tracking table",
  "flow": "vacation-care",
  "route": "index.html?v=0&step=forms&device=phone",
  "routeBare": "index.html?v=0&step=forms&device=phone&bare=1",
  "purpose": "Track per-child form completion for an activity and chase missing forms.",
  "entry": "From a Calendar activity chip (S1)",
  "exit": "Back to Calendar View",
  "platforms": [
    "web"
  ],
  "figma": null,
  "layout": [
    "Back link + activity title + date + Notify missing forms (n)",
    "Search (trailing teal circle) + status filters (All/Received/Missing)",
    "Table: select · Actions (view, send reminder) · Child's first/last name · Primary carer · Form name · Attached by · Status · Upload date"
  ],
  "copy": [
    {
      "element": "Back link",
      "text": "‹ Back to Calendar View"
    },
    {
      "element": "Notify button",
      "text": "Notify missing forms ({n})"
    },
    {
      "element": "Columns",
      "text": "Child’s first name · Child’s last name · Primary carer · Form name · Attached by · Status · Upload date"
    },
    {
      "element": "Status",
      "text": "Received / Missing"
    }
  ],
  "components": [
    "ds-input",
    "ds-selection-pill",
    "ds-checkbox",
    "ds-pill",
    "ds-btn"
  ],
  "tokens": [
    "--sd-colour-grey-50",
    "--sd-radius-lg"
  ],
  "redlines": [
    {
      "element": "Row padding",
      "spec": "14px 16px"
    },
    {
      "element": "Action button",
      "spec": "32px, teal outline"
    }
  ],
  "behaviour": [
    "Status filter narrows rows to All / Received / Missing.",
    "Missing rows show '—' for Attached by and Upload date (nothing attached).",
    "Notify sends a reminder; toast confirms the count.",
    "Per-row send-reminder is available; bulk-select ↔ Notify scope is an open question."
  ],
  "states": [
    "default",
    "filter:received",
    "filter:missing",
    "rows-selected",
    "empty filter result"
  ],
  "a11y": [
    "Row checkboxes + header select-all have accessible names.",
    "Action icon buttons (View form / Send reminder) have aria-labels.",
    "Status is text, not colour alone."
  ],
  "acceptance": [
    {
      "id": "S4-AC1",
      "text": "Missing rows never show an 'Attached by' name.",
      "assert": "row[status=missing] AttachedBy == '—' AND UploadDate == '—'",
      "screen": "S4"
    },
    {
      "id": "S4-AC2",
      "text": "Notify count equals the number of missing rows.",
      "assert": "Notify label count == count(rows[status=missing])",
      "screen": "S4"
    },
    {
      "id": "S4-AC3",
      "text": "Status filter narrows the table.",
      "assert": "filter=missing -> all visible rows status==missing",
      "screen": "S4"
    }
  ]
}
