{
  "$description": "Per-screen handoff spec for \"Educator — no shift (empty state)\" (S6) in flow \"xplor-hub-login\". Generated by scripts/build-handoff.mjs — do not edit by hand.",
  "id": "S6",
  "name": "Educator — no shift (empty state)",
  "flow": "xplor-hub-login",
  "route": "index.html?v=0&step=educator-noshift&device=phone",
  "routeBare": "index.html?v=0&step=educator-noshift&device=phone&bare=1",
  "purpose": "Empty state when the logged-in educator has no shift configured in rostering.",
  "entry": "Shown instead of S5 when the roster has no shift for the educator (prototype: reachable via the jump rail only — see open questions)",
  "exit": "Back → S1",
  "platforms": [
    "web"
  ],
  "figma": null,
  "layout": [
    "Hub shell with back-link 'Dashboard'; title 'Welcome Educator'; body vertically centred",
    "464px centred panel: heading · inline SVG illustration (roster card) · helper paragraph; no primary action"
  ],
  "copy": [
    {
      "element": "Back link",
      "text": "Dashboard"
    },
    {
      "element": "Title",
      "text": "Welcome Educator"
    },
    {
      "element": "Heading",
      "text": "No shifts to begin"
    },
    {
      "element": "Helper",
      "text": "To start a shift, make sure your centre administrator has one configured in the rostering."
    }
  ],
  "components": [
    "hub-shell",
    "hub-panel"
  ],
  "tokens": [
    "--sd-colour-grey-200",
    "--sd-colour-grey-1000",
    "--sd-colour-orange-100",
    "--sd-colour-cyan-500",
    "--sd-colour-grey-600"
  ],
  "redlines": [
    {
      "element": "Panel",
      "spec": "464px wide, 48px padding, centred text"
    },
    {
      "element": "Illustration",
      "spec": "120×96 inline SVG, token-coloured (no image asset)"
    }
  ],
  "behaviour": [
    "Terminal empty state — the only exit is the back link to the Dashboard.",
    "In the prototype this screen is reached via the left jump rail; the real Hub must branch here from login when the roster lookup returns no shift."
  ],
  "states": [
    "empty:no-shift"
  ],
  "a11y": [
    "The illustration is decorative — aria-hidden.",
    "The helper text carries the recovery instruction; keep it programmatically associated with the heading."
  ],
  "acceptance": [
    {
      "id": "S6-AC1",
      "text": "The empty state offers no shift actions.",
      "assert": "screen shows 'No shifts to begin' + helper text AND contains no End/Resume Shift controls",
      "screen": "S6"
    }
  ]
}
