# CODEOWNERS — path-based review gate: the system, not the artifacts.
#
# Day-to-day product/design ARTIFACTS (prototypes, handoffs, briefs, sandbox work,
# About/Playbook/Resources content) are intentionally NOT listed here — they self-merge
# once CI is green, so designers and PMs can do their jobs without waiting on review.
# What IS listed is everything that defines or guards the SYSTEM itself: changing these
# in fundamental ways should never happen without an owner knowing.
#
# Enforced via the "protect-main" ruleset ("Require review from Code Owners" is ON).
# Mason (admin) and Sam (write) are on the ruleset's BYPASS LIST (pull requests only),
# so each can merge his own PRs without waiting on the other — the gate exists to
# review OTHER contributors changing the system. Everyone still merges through PRs
# with green CI; bypass never permits force-pushes or branch deletion on main.
# ⚠️ Bypass is role-based: adding a new collaborator gives them write access AND
# bypass. If more PMs get push access, move bypass to an org team or remove the
# write-role bypass entry first.

# ── Design-system core — shared CSS (components, tokens, chrome), doc pages, exports ──
/docs/assets/css/              @MasonMaddy @samweinhandl-xplor
/docs/components/              @MasonMaddy @samweinhandl-xplor
/docs/tokens/                  @MasonMaddy @samweinhandl-xplor
/docs/api/                     @MasonMaddy @samweinhandl-xplor
/docs/llms.txt                 @MasonMaddy @samweinhandl-xplor

# ── System-definition pages — Foundations (personas, product suite, token architecture) ──
/docs/foundations/             @MasonMaddy @samweinhandl-xplor

# ── Site infrastructure — shared JS (nav.js carries the createElement-only security rule) ──
/docs/assets/js/               @MasonMaddy @samweinhandl-xplor

# ── Process layer — skill procedures, the agent contract, agent-facing product knowledge ──
/skills/                       @MasonMaddy @samweinhandl-xplor
/CLAUDE.md                     @MasonMaddy @samweinhandl-xplor
/context/                      @MasonMaddy @samweinhandl-xplor

# ── Guardrails — CI checks + generators, the workflow, and this file itself ──
/scripts/                      @MasonMaddy @samweinhandl-xplor
/.github/                      @MasonMaddy @samweinhandl-xplor
